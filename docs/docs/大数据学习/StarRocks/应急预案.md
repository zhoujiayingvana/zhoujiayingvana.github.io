| 突发情况类型 | 突发情况详情 | 应急方案 | 执行负责人 |
| --- | --- | --- | --- |
| 节点宕机 | fe节点宕机 | （1）优先重启节点<br/>（2）查看监控内存、cpu、是否异常<br/>（3）查看日志是否有`oom`、full gc，选举失败等字样，定位问题<br/>（4）优化jvm，调大jvm内存，调小be节点的mem_limit参数 | |
| | be节点宕机 | （1）优先重启节点<br/>（2）查看监控内存、cpu、剩余磁盘空间是否异常<br/>（3）查看日志是否有`emory exceed`、`core dumped`等字样，定位问题 | |
| fe元数据损坏或版本冲突 | FE 启动失败，提示 `metadata parse error` 或 `version mismatch`或`take too much time`<br/>某表查询报错：`too many versions`<br/>   | （1）定期进行元数据备份<br/>（2）尝试元数据恢复   | |
|  | 节点ip变更导致fe启动失败 | Follower FE:<br/>（1）将fe从集群中删除<br/>（2）清空metadata<br/>（3）重新将fe加入集群<br/>Leader FE：<br/>（1）若集群自动选举leader，则按follower fe的方式处理<br/>（2）若集群无法选举leader，则尝试元数据恢复 | |
| 数据被覆盖 | 分区被覆盖，分区数据丢失 | 从s3文件中导入备份数据 | |
| 查询变慢 |  查询报错：`tablet not found`、`too many versions`、`query timeout` | （1）查看资源组的资源使用情况<br/>（2）增大查询资源组的cpu和内存配置<br/>（3）减小其他资源组的配置，例如限制 Flink 写入并发、降低导入压力   | |
| 导入失败率上升 | Flink 报 `stream load failed` 或 `stream load timeout` | 调整 Flink Sink 参数，增大 `buffer flush rows`，增加写入数据间隔<br/>增加 BE `write_buffer_size` | |
| 资源占用过高 | cpu使用率过高 | （1）驱逐业务<br/>（2）集群扩容，申请新的EC2，大约需要半天 | |
| | 内存使用率过高 | （1）驱逐业务<br/>（2）集群扩容，申请新的EC2，大约需要半天 | |
| | 磁盘使用率过高 | 磁盘扩容，可直接扩大当前盘容量（无需其他操作），也可以新增卷（操作步骤如下）：<br/>（1）新增磁盘<br/>（2）设置新磁盘的be存储文件夹<br/>（3）修改be配置的<br/><font style="color:rgb(28, 30, 33);">storage_root_path，重启be</font> | |


